# AWS profile overrides for content-service
# When running with -Dspring.profiles.active=aws the app will pick these up.

# Disable Spring AI connectors on AWS if desired (left intentionally blank as
# we provide bean fallbacks for Ai clients when 'aws' profile is active).

